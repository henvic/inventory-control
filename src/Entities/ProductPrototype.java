package Entities;

public class ProductPrototype extends ProductAbstract {
    public ProductPrototype(String id, int price, String name, String vendor) {
        super(id, price, name, vendor);
    }
}
