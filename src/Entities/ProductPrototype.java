package Entities;

public class ProductPrototype extends ProductAbstract {
    public ProductPrototype(String id, int price, int amount, String name, String vendor) {
        super(id, price, amount, name, vendor);
    }
}
